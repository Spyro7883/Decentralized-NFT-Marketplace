{
  "name": "marketplacev4",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ marketplacev4",
    "create-local": "graph create --node http://localhost:8020/ marketplacev4",
    "remove-local": "graph remove --node http://localhost:8020/ marketplacev4",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 marketplacev4",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
